# Integration

This directory contains copy-paste snippets for bootstrapping the Quran module
inside another Flutter app. These files are references; move the pieces into
the host app's real `main.dart`, route table, and startup flow.
